# 页读 / Thusfar 2.0 进度

更新：2026-09-26，分支 `flutter-2.0`。本文件记录实际验收证据；阶段门槛未达到时保持该阶段进行中。

## A0 标准答案与测试骨架：进行中

| 门槛 | 当前证据 | 结果 |
|---|---|---|
| 378 个生产函数清单 | AST 生成 `docs/port/INVENTORY.md` 和 `inventory.json`，378 条；`generate_inventory.py --check` | 清单数量通过；类别仍需人工审计 |
| Python/Dart 语义差异 | Python 3.11 / Dart 共用 10,000 个 JSON、5,726 个 Unicode/数字/排序、700 个极值舍入、12 个哈希、4 个注入时间/随机、153 个正则位点/变体正反例及 26 个正则操作用例 | 进行中；正文偏移与并发位点仍需逐项确认 |
| 测试语料 | 5 部公有领域书、11 组边角 TXT/EPUB；1.7.x 完成、暂停及通过原版 HTTP 接口写入一条摘记的阿Q目录快照；`oracle/corpus/build.py --verify` 与 `annotate_snapshot.py --verify` | 166 个文件校验通过；缺历史人工多摘记及暂停态带摘记快照 |
| Python 标准答案录制 | 255 个 Python 测试两遍均通过；128 个选定纯函数中 123 个通用函数录得 1,655 个去重样本（含 43 个传播异常），其余 5 个以及 4 个含波动输入的函数有 9 份固定输入 special goldens。两遍 124 个原始输出文件逐字节一致，树 SHA-256 `14b854177fddfd4f7cbc09f5fc358272b6fafd8e97540f73ec86fb550508b7fa`，0 冲突；36 次无法用于通用录制的调用均登记了原因。其后新增了 4 个 oracle 专项测试，最终冻结树需再跑双遍。解析语料 16 项/19 文件、HTTP 60 例/27 路由族、历史整书产物 2 份已独立复验。阿Q 用指定模型录得 103 次真实传输（21 模型、82 免费 JEV），运行结果 9/9 段完成，154 个规范化整书文件在两次离线回放中逐字节一致；JS 折叠 20 截点也两次一致。前端折叠现在共 4 份各 20 截点。14 份合成传输录音带之外，阿Q 真实录音带已扫描，待与后续语料共用预算账本一起入库。另四部书尚无真实模型整书录音带。 | 未通过 |
| Dart 测试骨架 | `core/`、严格静态分析、CI 测试步骤、378 函数进度表；171 个原测试已逐项登记，其中 162 个按阶段待移植，9 个为 Python 工具专用例外。本机 `dart test` 记录 64 通过、549 跳过（378 函数 + 171 原测试）；业务断言 0 个已翻译 | 骨架通过；原测试翻译门槛未通过 |

**阶段判定：A0 未完成，不进入 A1。** `MANIFEST.yaml` 中 378 个函数目前都是 `pending_port`。上述录制和语义门槛通过后，再在此记录两次录制摘要、测试数和阶段结论。

`oracle/record/cost_estimate.py` 根据 Python 3.11 的实际分段和 `models.py` 费率生成 `cost-estimate.json`：五书共 237 段，仅首轮抽取约 ¥2.197，尚未计分类、小传、回顾和重试，超出 `PLAN.md` 的 ¥1 录制预算。真实请求受同一持久化金额账本限制。阿Q 一次完整录制的模型配置费率估值为 ¥0.127843，双倍费率守护账本记账 ¥0.255685；网关实际账单尚无收据，不能据此声称实付金额。现有阿Q、包法利夫人 1.7.x 缓存使用 `gemini-2.5-flash-lite` 和 `gpt-5.6-terra`，无原始 SSE 请求/回复，不能冒充指定模型录音带。

## 工具链与边界

- 当前本机 Flutter stable：**3.47.5**，Dart **3.13.4**；安装于 `/home/dev/.local/share/flutter`。2026-09-26 对照 Flutter 官方 Git `stable` 分支，远端与本机同为 `6a19cca56475dbfba1478ee68d7bd0c2ef891da1`。CI 使用 Flutter 官方 Dart stable 安装动作，尚未通过远端 CI 验证（本分支未推送）。
- Python 标准答案固定使用 Android 1.7.5 对应的 **Python 3.11**，本机为 3.11.13；系统的 Python 3.13 不用于录制。
- 当前仅执行 A0；未创建 Flutter App、未更换 HTTP 服务、未删除 Python 运行时、未打存档标签，也未推送或发布。

## 下一步

在 ¥1 累计守护预算内继续录制公开语料并入库，记录未覆盖项；完成新增测试后的最终函数双录。补齐整书、接口模型成功响应、每书 20 截点与原测试 Dart 断言，并复核全部 A0 门槛。
