# 页读 / Thusfar 2.0 进度

更新：2026-09-26，分支 `flutter-2.0`。本文件记录实际验收证据；阶段门槛未达到时保持该阶段进行中。

## A0 标准答案与测试骨架：进行中

| 门槛 | 当前证据 | 结果 |
|---|---|---|
| 378 个生产函数清单 | AST 生成 `docs/port/INVENTORY.md` 和 `inventory.json`，378 条；`generate_inventory.py --check` | 清单数量通过；类别仍需人工审计 |
| Python/Dart 语义差异 | Python 3.11 / Dart 共用 10,000 个 JSON、5,726 个 Unicode/数字/排序、700 个极值舍入、12 个哈希、4 个注入时间/随机用例；正则位点审计进行中 | 进行中；正文偏移与并发位点仍需逐项确认 |
| 测试语料 | 5 部公有领域书、11 组边角 TXT/EPUB；1.7.x 完成、暂停及通过原版 HTTP 接口写入一条摘记的阿Q目录快照；`oracle/corpus/build.py --verify` 与 `annotate_snapshot.py --verify` | 166 个文件校验通过；缺历史人工多摘记及暂停态带摘记快照 |
| Python 标准答案录制 | 函数、模型/JEV、整书产物、HTTP 路由和 Node 折叠录制器正在扩充；Python 3.11 录制器已在一次无网络运行中通过 216 个单测，采得 76 个纯函数的 1,291 条样本，重复输入输出冲突 0；52 个纯函数未触达，1,170 次不可编码类型或被测函数异常已显式登记；尚无覆盖完整语料且两次逐字节一致的正式 goldens | 未通过 |
| Dart 测试骨架 | `core/`、严格静态分析、CI 测试步骤、378 函数进度表；171 个原测试已逐项登记，其中 162 个按阶段待移植，9 个为 Python 工具专用例外 | 骨架通过；业务断言尚未翻译 |

**阶段判定：A0 未完成，不进入 A1。** `MANIFEST.yaml` 中 378 个函数目前都是 `pending_port`。上述录制和语义门槛通过后，再在此记录两次录制摘要、测试数和阶段结论。

按当前 `models.py` 的价格与五书实际分段估算，237 个首轮抽取请求约 ¥2.20，尚未计其他模型步骤，超出 `PLAN.md` 的 ¥1 录制预算。真实请求将限制在预算内并如实记录未覆盖的书；预算与完整语料门槛的冲突暂不作自行放宽。

## 工具链与边界

- 当前本机 Flutter stable：**3.47.5**，Dart **3.13.4**；安装于 `/home/dev/.local/share/flutter`。CI 使用 Flutter 官方 Dart stable 安装动作，尚未通过远端 CI 验证（本分支未推送）。
- Python 标准答案固定使用 Android 1.7.5 对应的 **Python 3.11**，本机为 3.11.13；系统的 Python 3.13 不用于录制。
- 当前仅执行 A0；未创建 Flutter App、未更换 HTTP 服务、未删除 Python 运行时、未打存档标签，也未推送或发布。

## 下一步

完成正则与 UTF-16 位点审计、真实摘记快照、全语料录制及二次逐字节比对；补齐 `SEMANTICS.md` 两边可运行的证据，复核 171 个测试的翻译门槛，然后按 `PLAN.md` 检查 A0。
